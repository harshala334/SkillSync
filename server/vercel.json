{
  "installCommand": "npm install --legacy-peer-deps",
  "version": 2,
  "builds": [
    { "src": "app.js", "use": "@vercel/node" }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/app.js" }
  ]
}